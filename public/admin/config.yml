# Decap CMS Configuration
# Auth: DecapBridge (production) + local_backend (development)

local_backend: true

backend:
  name: github
  repo: YOUR_GITHUB_USERNAME/YOUR_REPO_NAME    # TODO: Update with your actual GitHub repo
  branch: main
  # DecapBridge settings — update site_url after signing up at https://decapbridge.com
  # site_url: https://your-site.com
  # base_url: https://decapbridge.com

# Media settings
media_folder: "public/images/blog"
public_folder: "/images/blog"

# Slug settings
slug:
  encoding: "ascii"
  clean_accents: true
  sanitize_replacement: "-"

# Collections
collections:
  - name: "blog"
    label: "Blog Posts"
    folder: "content/blog/posts"
    create: true
    slug: "{{slug}}"
    extension: "mdx"
    format: "frontmatter"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Publish Date", name: "date", widget: "datetime", format: "YYYY-MM-DD" }
      - { label: "Featured Image", name: "featuredImage", widget: "image", required: false }
      - { label: "Excerpt", name: "excerpt", widget: "text" }
      - { label: "Author", name: "author", widget: "string", default: "Hormel Team" }
      - label: "Tags"
        name: "tags"
        widget: "list"
        field: { label: "Tag", name: "tag", widget: "string" }
      - { label: "Body", name: "body", widget: "markdown" }
